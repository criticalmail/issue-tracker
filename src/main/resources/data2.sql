DROP TABLE IF EXISTS COMMENT;

CREATE TABLE COMMENT (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY,
  issue BIGINT not null,
  by BIGINT not null,
  created DATE NOT NULL
);

ALTER TABLE COMMENT
ADD FOREIGN KEY (issue)
REFERENCES ISSUE(id);

ALTER TABLE COMMENT
ADD FOREIGN KEY (by)
REFERENCES USER(id);

INSERT INTO COMMENT (issue, by, created) VALUES (
	1, 
	1, 
	'2017-07-31'
);
